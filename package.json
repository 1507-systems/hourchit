{
  "name": "hourchit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Portable time-tracking, mileage, and invoicing for a one-person LLC. Cloudflare Worker + D1.",
  "license": "MIT",
  "scripts": {
    "profiles": "node scripts/generate-profiles.mjs",
    "predev": "npm run profiles",
    "dev": "wrangler dev",
    "pretest": "npm run profiles",
    "test": "vitest run",
    "pretest:watch": "npm run profiles",
    "test:watch": "vitest",
    "pretypecheck": "npm run profiles",
    "typecheck": "tsc --noEmit",
    "predeploy": "npm run profiles",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply DB --local",
    "migrate:remote": "wrangler d1 migrations apply DB --remote",
    "seed:sql": "node seed/generate.mjs"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "wrangler": "^3.84.0"
  }
}
